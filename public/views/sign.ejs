<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Conta</title>
    
    <link rel="shortcut icon" href="img/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="css/main.css">
    <link rel="stylesheet" href="css/sign.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

</head>
<body>
    
    <img class="logo" src="img/logo.png" alt="">

    <form id="form" action="/sign" method="post">

        <div class="info">
            
            <label for="user">Usuário</label>
            <label id="labelPhone" for="phone">Número (opcional)</label>

            <input class="form-control" id="user" name="user" type="text" value="<%= data.user %>" required>
            <input class="form-control" id="phone" name="phone"  onchange="resetBorder('phone')" value="<%= data.phone %>" type="tel" placeholder="DDD número (tudo junto)" min="11" max="11">

        </div>

        <label for="email">Email</label>
        <input class="form-control" onchange="resetBorder('email')" id="email" name="email" type="email" value="<%= data.email %>" required>

        <label for="pwd">Senha</label>
        <input class="form-control" id="pwd" name="pwd" type="password" value="<%= data.pwd %>" required>

        <label for="confirmPwd">Confirme a senha</label>
        <input class="form-control" onchange="resetBorder('confirmPwd')" id="confirmPwd" name="confirmPwd" value="" type="password" required>

        <input type="button" onclick="validate()" class="btn btn-primary button" value="Criar conta">
        <a class="btn btn-success" href="/login">Já tenho conta</a>

        <% try{ %>
            <% if(data.email != ""){ %>
                <span onclick="showMsnEmail()" id="iconEmail" class="material-icons"> error </span>
                <span id="msnEmail">email indisponivel</span>
            <% } %> 
        <% } catch(err){ console.log(err) } %> 

    </form>

    <script src="js/sign.js"></script>
</body>
</html>