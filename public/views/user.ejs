<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Criar Conta</title>
    
    <link rel="shortcut icon" href="../img/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link rel="stylesheet" href="../css/main.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

    <style>
        body{
            height: 100vh;
        }
        #img{
            width: 180px;
            height: 180px;
            border-radius: 50%;
            box-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            margin: 30px 0;
        }
        #board{
            position: relative;
            margin: auto;
            width: 400px;

            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 50px;
        }
        .info{
            display: grid;
            grid-template: 1fr 1fr / .23fr 1fr;
            justify-content: center;
            align-items: center;
            gap: 20px 0;
        }
        span{
            position: absolute;
            left: 0;
            top: 20px;
            cursor: pointer;
        }
        p{
            border-bottom: 2px solid rgba(0, 0, 0, 0.4);
            padding: 5px 10px;
            width: 100%;
            display: inline-block;
            margin: 0;
        }
    </style>

</head>
<body>

    <div id="board">

        <span class="material-icons mr-3 ml-3" onclick="history.back()"> arrow_back_ios </span>
        <img src="data:image/<%=img.contentType%>;base64, <%=img.data.toString('base64')%>" id="img" class="labelImg" onclick="clickLabel()">

        <div class="info w-100">

            <strong>Usúario: </strong><p><%=user.user%></p>
    
            <% if(user.phone){ %>
                <strong>Telefone: </strong> <p><%= user.phone %></p>
            <% } %>

        </div>



    </div>


</body>
</html>